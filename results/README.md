# Results

Store compact metrics, tables, plots and run manifests here. Keep large checkpoints and raw datasets outside version control.

Every result should retain an experiment ID, configuration path, seed, checkpoint identifier, software environment and completion status. Never overwrite a reported result; create a new experiment directory.
